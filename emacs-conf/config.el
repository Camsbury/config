;; -*- lexical-binding: t; -*-
(require 'prelude)

(m-require config
  performance
  transient-defaults
  theme
  search
  navigation
  env
  text
  prog
  info
  desktop
  dev
  langs
  modes
  services
  viewers
  games
  gtd
  utils)

(provide 'config)
