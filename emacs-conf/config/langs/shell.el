;; -*- lexical-binding: t; -*-
(require 'prelude)
;; sh-script owns this; declare so the load-time setq doesn't warn.
(declare-vars sh-basic-offset)
(setq sh-basic-offset 2)
(provide 'config/langs/shell)
